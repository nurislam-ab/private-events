<h1>All users</h1>

<% @users.each do |u| %>
<div class="user_block">
    <div class="user_field">
        <h3><%= link_to u.name, user_path(u.id) %></h3>
    </div>
    
    <div class="user_field">
        <h4><%= u.email %></h4>
    </div>
</div>
<% end %>